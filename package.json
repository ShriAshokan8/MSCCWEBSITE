{
  "name": "msc-initiative-website",
  "version": "1.0.0",
  "description": "MSC Initiative - A student-led STEM movement exploring Maths, Science, and Computing",
  "scripts": {
    "format:css": "postcss style.css --replace --use css-declaration-sorter",
    "minify:css": "postcss style.css -o style.min.css --use cssnano",
    "minify:js": "terser script.js -o script.min.js --compress --mangle && terser js/main.js -o js/main.min.js --compress --mangle && terser sw.js -o sw.min.js --compress --mangle",
    "minify": "npm run minify:css && npm run minify:js",
    "format": "npm run format:css",
    "build": "npm run format && npm run minify"
  },
  "keywords": [
    "STEM",
    "education",
    "mathematics",
    "science",
    "computing"
  ],
  "author": "MSC Initiative",
  "license": "Apache-2.0",
  "devDependencies": {
    "css-declaration-sorter": "^7.3.0",
    "cssnano": "^7.1.2",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "terser": "^5.44.1"
  }
}
